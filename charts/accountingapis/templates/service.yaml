apiVersion: v1
kind: Service
metadata:
  name: {{ template "accountingapis.fullname" . }}
  labels:
    app: {{ template "accountingapis.name" . }}
    chart: {{ template "accountingapis.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "accountingapis.name" . }}
    release: {{ .Release.Name }}

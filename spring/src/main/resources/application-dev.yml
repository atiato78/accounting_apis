camel:
  springboot:
    # The Camel context name
    name: CamelRestSql
  component:
    servlet:
      mapping:
        enabled: true
        context-path: /camel-rest-sql/*

# Binding health checks to a different port
management:
  port: 8081

# disable all management enpoints except health
endpoints:
  enabled: false
  health:
    enabled: true

spring:
  # Spring JDBC configuration
  datasource:
    url:      jdbc:mysql://192.168.0.104:3306/ra_systemdb?serverTimezone=EST5EDT
    username: root
    password: Oa03216287@
    
#    spring:
  # Spring JDBC configuration
 # datasource:
  #  url:      jdbc:mysql://${${mysql.service.name}.service.host}:${${mysql.service.name}.service.port}/${mysql.service.database}
  #  username: user7SS
  #  password: qLipb2eRpLsKdJLT
    

  # Spring JDBC configuration
test:
  datasource:
   url: jdbc:oracle:thin:@192.168.0.108:1521/orcl
   username: super_conf
   password: super_conf

# The application configuration properties
quickstart:
  generateOrderPeriod: 2s
  processOrderPeriod: 5s

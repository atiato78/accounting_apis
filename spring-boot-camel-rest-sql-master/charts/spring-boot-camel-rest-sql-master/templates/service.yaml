apiVersion: v1
kind: Service
metadata:
  name: {{ template "spring-boot-camel-rest-sql-master.fullname" . }}
  labels:
    app: {{ template "spring-boot-camel-rest-sql-master.name" . }}
    chart: {{ template "spring-boot-camel-rest-sql-master.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "spring-boot-camel-rest-sql-master.name" . }}
    release: {{ .Release.Name }}
